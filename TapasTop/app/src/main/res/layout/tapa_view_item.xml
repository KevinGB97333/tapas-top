<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="20dp"
        android:elevation="7dp"
        app:cardUseCompatPadding="true"
        app:cardBackgroundColor="@color/orange_100">
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="10dp"
            android:clipToPadding="false">

            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/tapa_img"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:layout_marginLeft="20dp"
                android:scaleType="centerCrop"
                android:layout_alignParentLeft="true"
                android:layout_centerVertical="true"
                android:elevation="10dp"
                android:clipToOutline="false"
                tools:src="@drawable/tapa_placeholder" />

            <TextView
                android:id="@+id/tapa_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_marginLeft="40dp"
                android:layout_toEndOf="@id/tapa_img"
                android:fontFamily="@font/brandon_grotesque_bold_italic"
                android:textSize="32sp"
                android:textStyle="bold"
                tools:text="Tortilla de patata" />
        </RelativeLayout>

    </androidx.cardview.widget.CardView>

</layout>